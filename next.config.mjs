/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["images.pexels.com", "images.unsplash.com", "plus.unsplash.com"], // ✅ Allow Pexels images
  },
};

export default nextConfig;
